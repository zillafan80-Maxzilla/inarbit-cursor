total_equity:
  threshold: 1000.0
max_drawdown:
  max_drawdown: 0.2
  peak_equity: 1000.0
exposure:
  limit: 1.0
  total_limit: 1.5
rebalancer:
  enabled: true
funding_rate:
  max_rate: 0.01
auto_transfer:
  mode: mock
panic:
  enabled: true
api_key_reload:
  watch_path: "config/api_keys.yaml"
